<a href="/dashboard/files" class="btn btn-primary mb-3"><i class="fas fa-angle-right mr-2"></i>قائمة الملفات</a>

<div class="card card-success card-outline">
    <div class="card-header d-flex align-items-center">
        <h3 class="card-title"><i class="fas fa-pencil-alt mr-2"></i>تعديل الملف</h3>
    </div>
    <form action="/dashboard/files/<%= file.id %>/update" method="post" enctype="multipart/form-data">
        <div class="card-body">
            <% messages.forEach(message => { %>
            <div class="alert alert-<%= message.variant || 'primary' %>" id="app-alert"><%- message.body %></div>
            <% }) %>

            <div class="alert alert-info">- اذا لم ترغب في تغير الملف اترك الحقل فارغ.</div>
            <div class="row">
                <div class="col-12 col-md-4 col-lg-4 col-xl-4">
                    <div class="form-group">
                        <label>الملف : </label>
                        <div class="custom-file">
                            <input type="file" class="custom-file-input" name="file" />
                            <label class="custom-file-label" data-browse="اختر ملف">-</label>
                        </div>
                    </div>
                </div>
                <div class="col-12 col-md-4 col-lg-4 col-xl-4">
                    <div class="form-group">
                        <label>وصف الملف :</label>
                        <input type="text" class="form-control" name="description" placeholder="" value="<%= vv('description', file.description) %>" />
                    </div>
                </div>
            </div>
        </div>
        <div class="card-footer">
            <button class="btn btn-success" type="submit">تعديل الملف</button>
        </div>
    </form>
</div>
