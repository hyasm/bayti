<a href="/dashboard/offers/<%= request.id %>" class="btn btn-primary mb-3"><i class="fas fa-angle-right mr-2"></i>قائمة العروض</a>

<div class="card card-danger card-outline">
    <div class="card-header">
        <h3 class="card-title"><i class="fas fa-times mr-2"></i>رفض العرض</h3>
    </div>
    <form action="/dashboard/offers/<%= request.id %>/<%= offer.id %>/reject" method="post">
        <div class="card-body">
            <% messages.forEach(message => { %>
            <div class="alert alert-<%= message.variant || 'primary' %>" id="app-alert"><%- message.body %></div>
            <% }) %>

            <p class="font-weight-bold">- سيتم رفض العرض ( <span class="text-primary"><%= offer.id %></span> ) من مقدم الخدمة ( <span class="text-primary"><%= offer.user.full_name %></span> ).</p>
            <p class="font-weight-bold">- يرجى كتابة سبب رفض العرض.</p>
            <p class="font-weight-bold">- لا يمكن التراجع عن هذا الإجراء بعد التنفيذ.</p>
            <div class="form-group">
                <label>سبب الرفض :</label>
                <textarea class="form-control" name="reason" rows="5"><%= vv("reason") %></textarea>
            </div>
            <div class="form-group mb-0">
                <label>تأكيد رفض العرض :</label>
                <div class="custom-control custom-radio">
                    <input class="custom-control-input" type="radio" id="agreement" name="agreement" value="yes" />
                    <label for="agreement" class="custom-control-label">تأكيد</label>
                </div>
            </div>
        </div>
        <div class="card-footer">
            <button class="btn btn-danger" type="submit">رفض العرض</button>
        </div>
    </form>
</div>
