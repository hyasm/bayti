<a href="/dashboard/files" class="btn btn-primary mb-3"><i class="fas fa-angle-right mr-2"></i>قائمة الملفات</a>

<div class="card card-danger card-outline">
    <div class="card-header d-flex align-items-center">
        <h3 class="card-title"><i class="fas fa-trash-alt mr-2"></i>حذف الملف</h3>
    </div>
    <form action="/dashboard/files/<%= file.id %>/delete" method="post">
        <div class="card-body">
            <% messages.forEach(message => { %>
            <div class="alert alert-<%= message.variant || 'primary' %>" id="app-alert"><%- message.body %></div>
            <% }) %>

            <p class="font-weight-bold">- سيتم حذف الملف ( <span class="text-primary"><%= file.id %></span> ) - ( <span class="text-primary"><%= file.description %></span> ) .</p>
            <p class="font-weight-bold">- لا يمكن التراجع عن هذا الإجراء بعد التنفيذ.</p>
            <div class="form-group mb-0">
                <label>تأكيد حذف الملف :</label>
                <div class="custom-control custom-radio">
                    <input class="custom-control-input" type="radio" id="agreement" name="agreement" value="yes" />
                    <label for="agreement" class="custom-control-label">تأكيد</label>
                </div>
            </div>
        </div>
        <div class="card-footer">
            <button class="btn btn-danger" type="submit">حذف الملف</button>
        </div>
    </form>
</div>
