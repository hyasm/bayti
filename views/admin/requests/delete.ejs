<a href="/dashboard/requests" class="btn btn-primary mb-3"><i class="fas fa-angle-right mr-2"></i>قائمة الطلبات</a>

<div class="card card-danger card-outline">
    <div class="card-header d-flex align-items-center">
        <h3 class="card-title"><i class="fas fa-trash-alt mr-2"></i>حذف الطلب</h3>
    </div>
    <form action="/dashboard/requests/<%= request.id %>/delete" method="post">
        <div class="card-body">
            <% messages.forEach(message => { %>
            <div class="alert alert-<%= message.variant || 'primary' %>" id="app-alert"><%- message.body %></div>
            <% }) %>
            
            <p class="font-weight-bold">- سيتم حذف الطلب ( <span class="text-primary"><%= request.id %></span> ).</p>
            <p class="font-weight-bold">- لا يمكن التراجع عن هذا الإجراء بعد التنفيذ.</p>
            <div class="form-group mb-0">
                <label>تأكيد حذف الطلب :</label>
                <div class="custom-control custom-radio">
                    <input class="custom-control-input" type="radio" id="agreement" name="agreement" value="yes" />
                    <label for="agreement" class="custom-control-label">تأكيد</label>
                </div>
            </div>
        </div>
        <div class="card-footer">
            <button class="btn btn-danger" type="submit">حذف الطلب</button>
        </div>
    </form>
</div>
